define([ 
		
		"jquery", 
		"backbone",
		"com/views/PageView",
	
	], function( $, Backbone, PageView ) {
		
    // Extends PageView class
    var ServicePageView = PageView.extend({
    	
    });

    // Returns the View class
    return ServicePageView;

});